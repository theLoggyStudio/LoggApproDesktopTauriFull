{
  "compilerOptions": {
    "types": ["node"],
    "noEmit": true,  // Empêche la sortie de fichiers .js
    "allowJs": true,  // Autorise les fichiers .js dans votre projet
    "checkJs": false,  // Désactive la vérification des erreurs dans les fichiers .js
    "esModuleInterop": true,
    "strict": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react",
    "target": "es6"
  },
  "include": [
    "./types/custom.d.ts"  // Spécifie les chemins des fichiers à inclure
    
  ],
  "exclude": [
    "**/*.ts",  // Exclut tous les fichiers .ts pour ne pas générer de .js
    "**/*.tsx"  // Exclut tous les fichiers .tsx pour la même raison
  ]
}
